{
	"name": "phpstan-playground-lambda",
	"version": "1.0.0",
	"description": "",
	"main": "handler.ts",
	"scripts": {
		"deploy": "serverless deploy",
		"check": "tsc -p tsconfig.json --noEmit && tslint -p tsconfig.json -c tslint.json"
	},
	"author": "",
	"license": "MIT",
	"dependencies": {
		"aws-sdk": "^2.316.0"
	},
	"devDependencies": {
		"@types/aws-lambda": "^8.10.13",
		"@types/node": "^10.10.1",
		"serverless": "^1.32.0",
		"serverless-offline": "^3.25.12",
		"serverless-plugin-typescript": "^1.1.5",
		"tslint": "^5.11.0",
		"typescript": "^3.0.3"
	}
}
